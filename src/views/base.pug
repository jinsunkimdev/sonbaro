doctype html
html(lang="ja")
    head 
        title #{pageTitle} | Sonbaro
        link(rel="stylesheet" href="https://unpkg.com/mvp.css@1.12/mvp.css")
        block script
    body
        header
            h1=pageTitle
            nav 
                ul 
                    li 
                        a(href="/") Home
                    li 
                        a(href="/restaurants/search") Search
                    if loggedIn
                        li
                            a(href="/users/logout") Logout
                        li 
                            p #{loggedInUser.name}
                    else
                        li
                            a(href="/join") Join
                        li 
                            a(href="/login") Login
        main 
            block content
    include partials/footer.pug